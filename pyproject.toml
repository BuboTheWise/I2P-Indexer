[project]
name = "i2p-indexer"
version = "0.4.13"
description = "Client-side discovery and cataloging tool for I2P eepsites"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "BuboTheWise"},
]

[tool.pytest.ini_options]
testpaths = ["tests"]
